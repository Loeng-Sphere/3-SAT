测试答案是否正确：

方法一：（需要装Python 3）

verifier.py的用法

在命令行里找到放verifier.py的文件夹，输入以下：
python verifier.py

然后先跟平常测试你自己的代码一样，输入所有资讯，然后再把你答案的1-0串输入；显示GOOD就是可以，BAD就是错了。

方法二：

到网站 onlinegdb.com，右上选项按 Python 3；
把verifier.py的代码复制上去，然后余下与上述方法一“然后”那步一致。